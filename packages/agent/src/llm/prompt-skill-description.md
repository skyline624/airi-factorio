You write a ONE-SENTENCE description of what a Factorio agent skill function does, for a searchable skill library.

Given the function's source code, output ONLY the single sentence — no code, no markdown, no function name, no mention of `ops.` or `state`. Describe the OUTCOME and the key targets/quantities, starting with a verb.

Examples:
- "Mines at least 10 iron ore, walking to the nearest patch and widening the search radius on failure."
- "Builds a basic smelting setup: places a burner mining drill on iron ore and a stone furnace on its output, then fuels both with coal."
- "Crafts 5 iron gear wheels, smelting more iron plates first if needed."

Output the sentence and nothing else.
