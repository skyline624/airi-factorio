export interface FactorioConfig {
  path: string
  savePath: string
  rconPassword: string
  rconPort: number
  configPath: string
}

export interface WsServerConfig {
  host: string
  port: number
}
